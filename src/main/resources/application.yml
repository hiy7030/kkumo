spring:
  h2:
    console:
      path: /h2
      enabled: true
    datasource:
      url: jdbc:h2:mem:test
    jpa:
      hibernate:
        ddl-auto: create
      properties:
        hibernate:
          format_sql: true
          show_sql: true